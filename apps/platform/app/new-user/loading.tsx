import { Loading as Spinner } from '@repo/global-ui';

const Loading = () => (
  <div className="flex h-screen w-screen items-center justify-center">
    <Spinner />
  </div>
);
export default Loading;
