import { Loading as Spinner } from '@repo/global-ui/index';

const Loading = () => (
  <div className="flex h-screen w-screen items-center justify-center">
    <Spinner />
  </div>
);
export default Loading;
